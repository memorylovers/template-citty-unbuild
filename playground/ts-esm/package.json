{
  "name": "ts-esm-sample",
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "build": "tsc main.ts --outDir ./dist --module nodenext",
    "test": "npm run build && node ./dist/main.js && npx template-citty-unbuild AAA"
  },
  "dependencies": {
    "template-citty-unbuild": "file:../../"
  },
  "devDependencies": {
    "typescript": "^5.7.2"
  }
}